# User task template

Copy and fill.

```text
OS: [macOS | Linux | Windows]
USER_ROOTS active:
  R-DL: [full path]
  R-DT: [full path]
  R-DOC: [full path]

Task: [inventory | dry-run | apply approved ids]
Root this turn: [R-DL or path]
Do not touch: [IDs from section B/C]
Verbosity: [EXPLAIN | QUIET MODE]

If apply: OK rows [P-001, P-002]
Permanent unlink: [none | row ids]
```
